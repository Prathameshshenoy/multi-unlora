[project]
name = "multi-unlora"
version = "0.1.0"
description = "Multi-UnLoRA"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "modal>=1.4.1",
]

[dependency-groups]
dev = [
    "black>=26.3.1",
]
